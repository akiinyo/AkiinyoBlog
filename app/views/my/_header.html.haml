.span8.offset2
  .icon= image_tag(current_user.twitter_icon_url, size: "48x48")
  %h1= "#{current_user.name}さんのマイページ"

  - if request.path_info == '/my'
    .write_button= link_to 'ブログをかく', new_my_post_path, class: 'btn btn-primary btn-primary'
