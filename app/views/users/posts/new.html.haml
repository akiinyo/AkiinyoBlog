%div
.span8.offset2
  = form_for [current_user, current_user.posts.build] do |f|
    = f.label 'タイトル'
    = f.text_field :title

    = f.label '画像'
    = f.file_field :photo

    = f.label '本文'
    = f.text_area :body

    = f.submit '投稿する', class:"btn btn-small btn-primary"
